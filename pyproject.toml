[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]   # explicit: repo root goes on sys.path, so `import src...` always resolves,
                      # regardless of __init__.py presence or which directory you run pytest from